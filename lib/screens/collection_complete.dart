import 'package:flutter/material.dart';

class CollectionComplete extends StatelessWidget {
  const CollectionComplete({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}